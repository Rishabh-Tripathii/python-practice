# OOP Basics
    Object Oriented Programming organises the data into **objects** through class.
    e.g: class Student:
            pass
       
# Instance Variables 
    Variables that are unique for each instances . for example each student will have different name so Student.name() will be different for each of them.

# Class Variables 
    Variables that are same for all objects in a same class . for example all the employees will have same school name so Student.school() will be same for each of them.

